{
  "name": "erumi-anime-streaming",
  "version": "2.5.0",
  "description": "Erumi – Teal Cinema Anime Streaming PWA & Android APK",
  "author": "Jhudel",
  "license": "MIT",
  "private": true,
  "scripts": {
    "cap:init": "npx cap init Erumi com.erumi.animestreaming --web-dir web",
    "cap:add-android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && gradlew assembleDebug",
    "cap:build-release": "npx cap sync android && cd android && gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
